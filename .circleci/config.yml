version: 2
jobs:
  build:
    docker:
      - image: circleci/node:12.13.0
    steps:
      - checkout
      - run: npm install
      - run: npm build
  test:
    working_directory: ~/CSC308/FrontEnd/Hopsy
    docker:
      - image: circleci/node:12.13.0
    steps:
      - checkout
      - run: npm install
      - run: npm test 
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test
